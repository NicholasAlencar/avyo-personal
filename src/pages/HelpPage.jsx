import { useState } from 'react'
import { Search } from 'lucide-react'
import { Card } from '../components/ui/Card'
import { PageHeader } from '../components/avyo/PageHeader'
const faqs = [['Onde meus dados ficam?', 'Somente neste navegador, usando armazenamento local.'], ['O AVYO movimenta meu dinheiro?', 'Não. Ele organiza informações e oferece simulações educativas.'], ['Posso importar meu extrato?', 'Sim, pela página Transações usando um arquivo CSV.'], ['As projeções são garantidas?', 'Não. Taxas e valores são cenários para aprendizado.']]
export function HelpPage() { const [query, setQuery] = useState(''); const items = faqs.filter((f) => f.join(' ').toLowerCase().includes(query.toLowerCase())); return <><PageHeader eyebrow="Aprender" title="Ajuda" subtitle="Respostas diretas para usar o AVYO com segurança." /><label className="flex items-center gap-3 rounded-xl border border-white/10 bg-white/[0.04] px-4"><Search size={18} className="text-slate-500" /><span className="sr-only">Buscar ajuda</span><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Busque uma dúvida" className="min-h-12 flex-1 bg-transparent outline-none" /></label><div className="mt-5 space-y-3">{items.map(([q, a]) => <Card key={q} className="p-5"><h2 className="font-heading font-semibold">{q}</h2><p className="mt-2 text-sm text-slate-400">{a}</p></Card>)}</div></> }
