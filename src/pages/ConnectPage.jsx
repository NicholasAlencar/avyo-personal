import { Building2, FileSpreadsheet, ShieldCheck } from 'lucide-react'
import { Card } from '../components/ui/Card'
import { PageHeader } from '../components/avyo/PageHeader'
export function ConnectPage() { return <><PageHeader eyebrow="AVYO Connect" title="Seus dados, no seu controle" subtitle="Esta versão funciona sem contas bancárias conectadas e sem enviar dados para servidores." /><Card className="overflow-hidden p-7"><div className="grid gap-6 md:grid-cols-[1.3fr_1fr]"><div><span className="inline-flex rounded-full bg-emerald-400/10 px-3 py-1 text-xs font-semibold text-emerald-300">Modo local ativo</span><h2 className="mt-4 font-heading text-2xl font-bold">Privacidade por padrão</h2><p className="mt-3 leading-relaxed text-slate-400">As conexões bancárias automáticas não estão disponíveis nesta versão. Use CSV para trazer movimentações quando quiser.</p></div><div className="grid gap-3">{[[ShieldCheck, 'Nada sai do navegador'], [FileSpreadsheet, 'Importação CSV local'], [Building2, 'Open Finance no roadmap']].map(([Icon, text]) => <div key={text} className="flex items-center gap-3 rounded-xl bg-white/[0.04] p-3 text-sm text-slate-300"><Icon size={18} className="text-cyan-300" />{text}</div>)}</div></div></Card></> }
