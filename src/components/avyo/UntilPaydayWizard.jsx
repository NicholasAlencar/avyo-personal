import { useState } from 'react'
import { Button } from '../ui/Button'

const input = 'mt-2 min-h-11 w-full rounded-xl border border-white/10 bg-white/[0.04] px-3 outline-none focus:border-cyan-400/50'
export function UntilPaydayWizard({ onSave }) { const [form, setForm] = useState({ balance: '', nextPaymentDate: '', safetyReserve: '', planned: '' }); const set = (key) => (e) => setForm((v) => ({ ...v, [key]: e.target.value })); return <form onSubmit={(e) => { e.preventDefault(); onSave({ balance: Number(form.balance), nextPaymentDate: form.nextPaymentDate, safetyReserve: Number(form.safetyReserve), plannedItems: form.planned ? [{ id: 'planned-main', description: 'Despesas planejadas', amount: Number(form.planned) }] : [], status: 'active' }) }} className="avyo-card max-w-xl p-6"><h2 className="font-heading text-xl font-bold">Monte seu plano até receber</h2><p className="mt-2 text-sm text-slate-400">Separe o que já tem destino e descubra sua margem diária.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm text-slate-300">Saldo disponível<input className={input} type="number" required value={form.balance} onChange={set('balance')} /></label><label className="text-sm text-slate-300">Próximo recebimento<input className={input} type="date" required value={form.nextPaymentDate} onChange={set('nextPaymentDate')} /></label><label className="text-sm text-slate-300">Despesas planejadas<input className={input} type="number" value={form.planned} onChange={set('planned')} /></label><label className="text-sm text-slate-300">Reserva de segurança<input className={input} type="number" value={form.safetyReserve} onChange={set('safetyReserve')} /></label></div><Button type="submit" className="mt-6">Criar meu ritmo</Button></form> }
