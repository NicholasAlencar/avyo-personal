import { formatCurrency } from '../../lib/format'
import { Card } from '../ui/Card'
export function InvestmentPortfolio({ investments }) { return <div className="space-y-3">{investments.map((item) => <Card key={item.id} className="flex items-center gap-4 p-5"><div className="grid size-11 place-items-center rounded-xl bg-violet-400/10 font-heading text-violet-300">{item.category?.[0] || 'I'}</div><div className="min-w-0 flex-1"><h3 className="truncate font-medium">{item.name}</h3><p className="text-xs text-slate-500">{item.institution} · {item.category}</p></div><div className="text-right"><strong>{formatCurrency(item.currentValue)}</strong><p className={`text-xs ${item.currentValue >= item.investedValue ? 'text-emerald-300' : 'text-rose-300'}`}>{((item.currentValue / item.investedValue - 1) * 100).toFixed(1)}%</p></div></Card>)}</div> }
