import { useState } from 'react'
import { formatCurrency } from '../../lib/format'
export function NextContribution({ profile }) { const [value, setValue] = useState(1000); const fixed = profile === 'conservador' ? .75 : profile === 'arrojado' ? .25 : .5; return <div className="avyo-card p-6"><h2 className="font-heading text-xl font-semibold">Próximo aporte</h2><label className="mt-5 block text-sm text-slate-300">Quanto você pretende aportar?<input type="number" value={value} onChange={(e) => setValue(Number(e.target.value))} className="mt-2 min-h-11 w-full max-w-xs rounded-xl border border-white/10 bg-white/[0.04] px-3" /></label><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-white/[0.04] p-4"><p className="text-xs text-slate-500">Base estável</p><strong>{formatCurrency(value * fixed)}</strong></div><div className="rounded-xl bg-white/[0.04] p-4"><p className="text-xs text-slate-500">Diversificação</p><strong>{formatCurrency(value * (1 - fixed))}</strong></div></div><p className="mt-4 text-xs text-slate-500">Exemplo educativo. Considere custos, objetivos e sua reserva antes de decidir.</p></div> }
