import { useFinanceStore } from '../../context/FinanceContext'
import { Button } from '../ui/Button'
import { Card } from '../ui/Card'
export function SuitabilityQuiz() { const { state, replaceState } = useFinanceStore(); const choose = (profile) => replaceState({ ...state, investmentProfile: { answers: { selected: profile }, profile } }); return <Card className="p-6"><p className="text-xs font-semibold uppercase tracking-wider text-cyan-300">Seu jeito de investir</p><h2 className="mt-2 font-heading text-2xl font-bold">Seu perfil é {state.investmentProfile.profile}</h2><p className="mt-2 text-sm text-slate-400">Escolha a frase que mais combina com você hoje. Isso é educativo e pode mudar com o tempo.</p><div className="mt-6 grid gap-3 md:grid-cols-3"><Button variant="secondary" onClick={() => choose('conservador')}>Proteger primeiro</Button><Button variant="secondary" onClick={() => choose('equilibrado')}>Equilibrar risco</Button><Button variant="secondary" onClick={() => choose('arrojado')}>Aceitar oscilações</Button></div></Card> }
